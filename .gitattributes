# Git Large File Storage (Git LFS) configuration for large files
# Specify folders and file types to be tracked by Git LFS

# Voting Minutes PDFs - Scraper
scraper/scrape/data/voting_minutes_pdfs/*.pdf filter=lfs diff=lfs merge=lfs -text
